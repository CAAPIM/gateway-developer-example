
group = "mqnative"
version = '1.0.0'

GatewaySourceConfig {
    EnvironmentConfig {
        name = "RELEASE"
        map = [
                "SSG_ACTIVE_CONNECTOR": file("./src/main/gateway/config/active-connectors.yml")
        ]
    }
}
