
group = "mqnative"
version = '1.0.0'

GatewaySourceConfig {
    EnvironmentConfig {
        name = "mqnativeExample"
        map = [
                "SSG_ACTIVE_CONNECTOR": file("./src/main/gateway/config/active-connectors.yml")
        ]
    }
}
