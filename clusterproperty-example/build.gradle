
group = "clusterproperty"
version = '1.0.0'

GatewaySourceConfig {
    EnvironmentConfig {
        name = "SNAPSHOT"
        map = [
                "PROPERTY": file("./src/main/gateway/config/global-env.properties")
        ]
    }
}
