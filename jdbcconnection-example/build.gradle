
group = "jdbcconnection"
version = '1.0.0'

GatewaySourceConfig {
    EnvironmentConfig {
        name = "SNAPSHOT"
        map = [
                "JDBC_CONNECTION": file("./src/main/gateway/config/jdbc-connections.yml")
        ]
    }
}

