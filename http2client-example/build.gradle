
group = "http2client"
version = '1.0.0'

GatewaySourceConfig {
    EnvironmentConfig {
        name = "http2clientExample"
        map = [
                "GENERIC": file("./src/main/gateway/config/generic-entities.yml")
        ]
    }
}
