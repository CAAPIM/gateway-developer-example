
group = "http2client"
version = '1.0.0'

GatewaySourceConfig {
    EnvironmentConfig {
        name = "SNAPSHOT"
        map = [
                "GENERIC": file("./src/main/gateway/config/generic-entities.yml")
        ]
    }
}
